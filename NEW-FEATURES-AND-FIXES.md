# مهام جديدة — تنبيهات AI، تحويل للدعم، وإصلاحات الواتساب

> **Status legend**: ⬜ Not started · 🟡 In progress · ✅ Done
> كل مهمة هنا مستقلة عن التانية. الشغل بيتعمل مهمة مهمة، وكل مهمة بتتحدث هنا (Status + Notes) أول ما تخلص أو يحصل فيها تقدم.

## Status at a Glance

| ID | الاسم | Status | ملاحظات |
|---|---|---|---|
| F1 | تنبيه واتساب لما موديل AI يخلص limit | ✅ Done | الرقم بقى من `.env` (`GEMINI_ALERT_WHATSAPP_NUMBER`/`GEMINI_ALERT_BOT_ID`) والإرسال بقى متجدول كل دقيقة + الـ payload اتصلح ليطابق `/send-message` الحقيقي |
| F2 | تحويل لدعم فني + tab "منتظر الرد" في الداشبورد | ✅ Done (نسخة أولى) | كشف الطلب + fallback فشل → `status=awaiting_agent` + AI بيسكت، وصفحة داشبورد جديدة "الرسائل المنتظر الرد عليها" فيها رد + إنهاء تحويل |
| F3 | حد أقصى لعدد شهور التقسيط | ✅ Done | حساب القسط بقى بيتحقق من `installment_systems.plans` الفعلية للمكنة قبل ما يحسب؛ لو الرقم مش موجود بيرفض ويعرض المدد المتاحة |
| F4 | البوت مش بيحدد موديل المكنة كويس | ✅ Done | السبب الحقيقي اتلقى ومش زي ما كان متوقع في التحليل الأول — تفاصيل تحت |
| F5 | التقديم بيطلب الاسم أكتر من مرة | ✅ Done | قاعدة استخراج صريحة لـ full_name/national_id/phone اتضافت للـ prompt |

---

## F1 — تنبيه واتساب لما موديل AI يخلص الـ limit

**الطلب:** لما موديل AI يضرب limit، تتبعت رسالة واتساب على رقم من `.env` بتقول اسم الموديل اللي خلص.

**الوضع الحالي (موجود فعلاً، لكن معطّل):**
- فيه نظام تنبيهات جاهز في [`GeminiAlertService.php`](app/Services/GeminiAlertService.php) بيتنادى من [`GeminiClient.php:40`](app/Services/GeminiClient.php:40) لما كل الـ Gemini keys/models تفشل بـ 429، وبيسجل تنبيه في جدول `gemini_alerts` فيه أسماء الموديلات اللي فشلت.
- **المشكلة 1:** الرقم اللي بيتبعتله التنبيه متثبت (hardcoded) في [`config/gemini.php:215`](config/gemini.php:215) كـ `'2010XXXXXXXX'` — مش قاري من `.env` خالص.
- **المشكلة 2:** الدالة اللي فعليًا بتبعت الرسالة على واتساب (`GeminiAlertService::repeatOpenAlerts()`) **مش متجدولة في أي Scheduler** — يعني التنبيه بيتسجل في الداتابيز بس محدش بيبعته فعليًا. الجدولة الوحيدة الموجودة في [`routes/console.php`](routes/console.php) هي `gemini:reset-usage`.
- التنبيه الحالي بيتبعت على "كل المفاتيح خلصت" مش على "موديل معين خلص" — الطلب الجديد أدق (اسم الموديل اللي خلص تحديدًا).

**المطلوب تنفيذه:**
1. إضافة `GEMINI_ALERT_WHATSAPP_NUMBER` (أو اسم مشابه) في `.env`/`.env.example` وربط `config('gemini.alerts.whatsapp_number')` بيه بدل الرقم الثابت.
2. جدولة `GeminiAlertService::repeatOpenAlerts()` في `routes/console.php` (كل دقيقة أو دقيقتين، حسب `repeat_every_minutes`).
3. مراجعة نص الرسالة (`buildAllKeysExhaustedMessage`) للتأكد إنه بيوضح اسم الموديل بشكل واضح (هو بيعرض `key_id`/`model` بالفعل، محتاج مراجعة بس).
4. اختبار حي: محاكاة 429 والتأكد إن الرسالة توصل فعلاً على رقم الواتساب.

---

## F2 — تحويل لدعم فني + tab "الرسائل المنتظر الرد عليها"

**الطلب:**
- لو العميل طلب يتواصل مع الدعم الفني، أو طلب يوصله شخص حقيقي، أو الـ AI مش قادر يساعده — تتحط المحادثة في أرشيف وتظهر في tab جديدة في الداشبورد اسمها "الرسائل المنتظر الرد عليها".
- لما موظف الدعم يرد على العميل، هو اللي يكمل معاه (يعني الـ AI يوقف يرد على المحادثة دي لحد ما الموظف يقفلها/يرجعها للـ AI).

**الوضع الحالي (مفيش حاجة من ده موجودة):**
- `whatsapp_conversations` عندها عمود `status` (افتراضي `'open'`) بس مش مستخدم لأي منطق تحويل/أرشفة حاليًا.
- مفيش أي Filament resource أو صفحة بتعرض المحادثات/الرسائل نفسها — [`WhatsappBotResource`](app/Filament/Resources/WhatsappBotResource.php) بيدير الـ bots نفسها (QR، اتصال...) مش المحادثات.
- [`WhatsappIntentRouter`](app/Services/WhatsappIntentRouter.php) مفيهوش أي intent اسمه "دعم فني" أو "human support" — كل الـ intents الموجودة تجارية (سعر، تقسيط، تقديم...).

**المطلوب تنفيذه (يحتاج تصميم منفصل قبل التنفيذ):**
1. **كشف الطلب:** إضافة intent/heuristic جديدة في `WhatsappIntentRouter`/`AiIntentClassifier` تكتشف: طلب صريح للتواصل مع دعم/شخص حقيقي، أو حالة "الـ AI معرفش يرد" (fallback فشل أو classify رجّع نية غير مفهومة بشكل متكرر).
2. **حالة جديدة للمحادثة:** استخدام عمود `status` الموجود (أو عمود جديد `handoff_status`) بقيمة زي `awaiting_agent` بدل `open`، مع وقف الـ AI عن الرد على أي رسالة جديدة من نفس المحادثة طول ما هي في الحالة دي.
3. **Tab جديدة في الداشبورد:** صفحة Filament جديدة (أو tab جوه resource موجود) بتعرض كل المحادثات اللي `status = awaiting_agent`، مرتبة بآخر رسالة، مع رابط لعرض تاريخ المحادثة والرد منها مباشرة.
4. **رد الموظف:** لما الموظف يرد من الداشبورد، الرسالة تتبعت عن طريق نفس آلية `/send-message` الموجودة (زي `sendInstantAck` في `ApplicationHandler`)، والمحادثة ترجع لحالة عادية (أو تفضل مع الموظف) حسب ما هيتقرر في التصميم.
5. قرارات محتاجة توضيح وقت التصميم: هل الموظف بيقفل المحادثة يدويًا عشان ترجع للـ AI؟ هل فيه إشعار (واتساب/داشبورد) لما محادثة جديدة تدخل الـ tab؟

---

## F3 — حد أقصى لعدد شهور التقسيط

**الطلب:** منع العميل من كتابة رقم شهور عشوائي (من دماغه) — لازم يختار من مدد التقسيط المتاحة فعليًا.

**مثال حي من محادثة واتساب حقيقية (2026-08-21):**
```
العميل: علي 500 شهر
البوت: تمام يا فندم، القسط على 500 شهر :
- دايو ٤: 840 جنيه شهريًا
- دايو ٤ اصلي: 952 جنيه شهريًا
المصاريف الإدارية:
- دايو ٤: 3,150 جنيه
- دايو ٤ اصلي: 3,570 جنيه
```
البوت حسب قسط فعلي لمدة **500 شهر** (أكتر من 41 سنة) من غير أي اعتراض أو تنبيه — رقم غير منطقي بالمرة قبلته الحسبة عادي.

**الوضع الحالي (من الكود):**
- حقل `months` في فورم إدارة خطط التقسيط بالداشبورد ([`InstallmentSystemResource.php:43`](app/Filament/Resources/InstallmentSystemResource.php:43)) عبارة عن `TextInput` عادي، من غير `numeric()`, `maxValue()`, أو أي تحقق — ده بتاع الأدمن مش العميل.
- المشكلة الفعلية (من المثال) في مسار حساب القسط اللي بيتنادى من الواتساب (`installment_calc` intent في `WhatsappIntentRouter`) — البوت بياخد أي رقم شهور مكتوب من العميل ويحسب عليه القسط مباشرة، من غير ما يتأكد إن الرقم ده من ضمن خطط التقسيط الفعلية المعرّفة للمكنة دي في `installment_systems`/`InstallmentSystemResource` (اللي فيها مدد محددة، زي 12، 18، 24 شهر... مش أي رقم).
- نفس المنطق ينطبق على `installment_months` اللي بيتستخرج في `ApplicationHandler` وقت التقديم الفعلي — لازم يتقارن بالخطط المتاحة للمكنة قبل ما يتقبل.

**المطلوب تنفيذه:**
1. تحديد مكان حساب القسط في `WhatsappIntentRouter` (intent = `installment_calc`) وإضافة تحقق: الرقم اللي العميل كتبه لازم يكون من ضمن `months` الموجودة فعليًا في خطط التقسيط (`installment_systems`) الخاصة بالمكنة، مش أي رقم حر.
2. لو الرقم مش من ضمن الخطط المتاحة، الرد يوضح المدد المتاحة فعليًا ويطلب من العميل يختار منها (بدل ما يحسب رقم وهمي).
3. نفس التحقق في `ApplicationHandler::missingFields`/الاستخراج، عشان طلب التقديم النهائي مايتسجلش بعدد شهور مش موجود أصلاً كخطة.
4. (اختياري، أقل أولوية) إضافة `numeric()`/`maxValue()` على حقل `months` في `InstallmentSystemResource` نفسه كحماية إضافية وقت ما الأدمن بيضيف خطة جديدة.

---

## F4 — البوت مش بيحدد موديل المكنة كويس ✅

**السبب الحقيقي (اتأكد بتشغيل `MachineSearchService::debug()` مباشرة، مش تخمين):**
في [`MachineSearchService.php`](app/Services/MachineSearchService.php) وفي [`WhatsappBotController.php`](app/Http/Controllers/Api/WhatsappBotController.php) فيه جدول تطبيع نصوص (normalization) فيه سطر غلط:
```php
'دايو' => 'دايون',
'الدايو' => 'دايون',
```
ده بيحوّل أي رسالة فيها كلمة "**دايو**" (براند حقيقي موجود في الداتابيز، id=3) إلى "**دايون**" (اسم منتج تاني تمامًا، "Dayun D Max"/"Dayun Tx 250"). النتيجة: أي طلب فيه "دايو" كان بيتوه ويدور على حاجة تانية خالص، وده اللي فسّر رجوع هوجان/فيجوري بدل دايو، والتناقض في المثال التاني (المكنة كانت موجودة فعلاً بس البحث كان بيدور بالاسم الغلط).

**الحل المُطبّق:** شيل السطرين الغلط من الجدولين، مع الاحتفاظ بباقي الـ mappings الصحيحة (`dayun`, `daion`, `ديوان` لسه بتترجم لـ "دايون" بشكل صحيح لأنها فعلاً بتاعت نفس المنتج). اتأكد بعد التصحيح إن نفس المثالين رجعوا يطابقوا "دايو" فقط.

كمان اتضاف تصحيح تكميلي: لو الرسالة طلب براند بس (زي "عاوز مكنه دايو")، البوت دلوقتي بيعمل بحث جديد مباشر بدل ما يعتمد على مكن ممكن تكون فاضلة من سياق محادثة سابق (حماية إضافية غير مرتبطة بالسبب الأساسي).

**ملاحظة:** التحليل الأول في الملف (تخمين إن `str_contains('استيراد')` بيتلخبط مع "استيراد فرز تاني") اتراجع عنه بعد قراءة الكود بالتفصيل — الدالة `narrowMachinesByVariant()` بالفعل بتستثني "فرز تاني" صح ([`WhatsappIntentRouter.php:1509-1519`](app/Services/WhatsappIntentRouter.php:1509)). سايبها هنا كتوثيق إن التخمين الأول كان غلط والسبب الحقيقي مختلف.

---

## F4-تفاصيل (الأصلية، محفوظة للسياق)

**الطلب:** البوت بيتلخبط في تحديد الموديل الصح للمكنة (يشمل التفريق بين "استيراد" و"استيراد فرز تاني" كحالة خاصة، لكن المشكلة أعم من كده).

**مثال حي من محادثة واتساب حقيقية (2026-08-21):**
```
العميل: عاوز مكنه دايو
البوت: - هوجان هوجن 3 استيـراد: 50,000 جنيه
       - هوجان هوجن 3 استيراد فرز تاني: 40,000 جنيه
       - فيجوري فيجــوري 3: 39,000 جنيه
       تحب تشوف صور اي موديل فيهم؟
```
العميل طلب موديلات "**دايو**" والبوت رجّع موديلات "**هوجان**" و"**فيجوري**" — براندات مختلفة تمامًا، مالهاش أي علاقة بالطلب.

```
العميل: دايو ٤ اصلي بكام
البوت: الموديل ده مش متوفر من {دايو}.
       لكن {هوجان هوجن 3 استيـراد ، هوجان هوجن 3 استيراد فرز تاني ، فيجوري فيجــوري 3} متوفر عندنا.
       والموديلات المتاحة حاليًا من {دايو} هي:
       {- دايو ٤ ... - دايو ٤ اصلي ... }
```
هنا تناقض واضح: البوت بيقول "الموديل ده (دايو ٤ اصلي) مش متوفر من دايو"، وفي نفس الرسالة تحت بند "الموديلات المتاحة حاليًا من دايو" بيسرد "**دايو ٤ اصلي**" نفسه كموديل متاح! يعني المكنة موجودة فعلاً في قاعدة البيانات تحت براند دايو، لكن مسار البحث اللي بيحدد "هل الموديل ده متاح؟" فشل يلاقيها رغم إنها موجودة.

**تحليل الكود (تشخيص أولي، محتاج تأكيد بالـ debug tool الموجود):**
- الرد ده بيتبني في [`WhatsappIntentRouter.php:156-163`](app/Services/WhatsappIntentRouter.php:156) لما `filterMachinesByRequestedBrand()` تلاقي إن العميل طالب براند معين (`brand_requested = true`) بس مفيش ولا مكنة من الـ `$machines` (الناتجة من `resolveMachinesFromPlan()`) بتطابق البراند ده — فبيعرض بدلها `original_machines` (اللي هي أصلاً نتيجة بحث تاني مش ليها علاقة بالبراند المطلوب) على إنها "الأقرب المتاح"، وده اللي بيطلع هوجان/فيجوري بدل دايو.
- `MachineSearchService` عنده [`debug()`](app/Services/MachineSearchService.php:58) method جاهزة بترجع `query_tokens`, `family_names`, و`scores` — دي أسرع طريقة نتأكد بيها ليه "دايو ٤ اصلي" ماتطابقتش صح في `familyMatches()`/`scoreMachine()` (احتمالات: فرق فى الـ normalization بين "دايو ٤ اصلي" و اسم المكنة الفعلي في الداتابيز زي "دايو ٤ الاصلي"، أو مشكلة في `detectBrandOnlyToken()`).
- التمييز بين "استيراد" و"استيراد فرز تاني" جزء من نفس المشكلة الأعم: بما إن `'استيراد'` substring من `'استيراد فرز تاني'`، أي `str_contains($text, 'استيراد')` (موجودة في [`WhatsappIntentRouter.php:1458`](app/Services/WhatsappIntentRouter.php:1458) وأماكن تانية سطر 753، 1022) بتتطابق مع الاتنين مع بعض.

**المطلوب تنفيذه:**
1. تشغيل `MachineSearchService::debug('دايو')` و `debug('دايو ٤ اصلي')` (أو عن طريق tinker/route مؤقت) لمعرفة السبب الدقيق وراء فشل المطابقة قبل أي تعديل.
2. تصحيح منطق `familyMatches()`/`scoreMachine()` عشان "دايو ٤ اصلي" (وأي موديل موجود فعليًا تحت البراند) يتطابق صح.
3. تصحيح `filterMachinesByRequestedBrand()` عشان مايعرضش مكن من براند تاني تمامًا (هوجان/فيجوري) كـ"بديل" لطلب براند محدد (دايو) — البديل المعروض لازم يكون من نفس البراند على الأقل، أو الرد يوضح إن مفيش تطابق فعلاً بدل اقتراح براند تاني بدون توضيح.
4. تصحيح كل الأماكن اللي بتستخدم `str_contains($text, 'استيراد')` عشان تفرّق "استيراد" عن "استيراد فرز تاني" (مثلاً: تتأكد إن مفيش "فرز تاني" في النص قبل التصنيف كـ"استيراد" عادي).

---

## F5 — التقديم بيطلب الاسم أكتر من مرة

**الطلب:** فورم التقديم (application) بيسأل عن الاسم أكتر من مرة حتى لما العميل يبعته.

**مثال حي من محادثة واتساب حقيقية (2026-08-21):**
```
البوت: تمام يا فندم، ناقصني البيانات دي عشان أكمل طلب التقديم:
       - الاسم بالكامل
       - الرقم القومي ... (باقي الحقول)
العميل: كيرلس ناجي
البوت: [نفس القائمة كاملة تاني، بما فيها "الاسم بالكامل"]
العميل: الاسم كيرلس ناجي فهيم
البوت: [نفس القائمة كاملة تاني، بما فيها "الاسم بالكامل"]
```
العميل بعت الاسم مرتين بصيغتين مختلفتين ("كيرلس ناجي" ثم "الاسم كيرلس ناجي فهيم") والبوت في المرتين اعتبره **غير موجود** وسأل نفس القائمة الكاملة تاني.

**السبب اتحدد من الكود (مش تخمين، ده تأكيد):**
في [`AiIntentClassifier::extractApplicationData()`](app/Services/AiIntentClassifier.php:374) الـ prompt اللي بيتبعت للـ AI عشان يستخرج البيانات فيه قواعد صريحة ومفصّلة لكل حقل تقريبًا (`work_address`, `home_address`, `job_type`, `income_proof` — سطور 409-430) **إلا `full_name` و`national_id` و`phone`، مفيش أي قاعدة استخراج ليهم خالص**. يعني لما العميل يبعت رسالة قصيرة زي "كيرلس ناجي" أو حتى "الاسم كيرلس ناجي فهيم"، الـ AI معندوش أي توجيه إنه يفهم إن الرسالة القصيرة دي هي إجابة على سؤال "الاسم بالكامل" اللي اتسأل قبلها بالظبط — فبيرجّع `full_name: null` والسؤال بيتكرر.

**المطلوب تنفيذه:**
1. إضافة قاعدة صريحة في prompt الـ `extractApplicationData()` تقول: لو آخر رد من البوت كان بيسأل عن حقل معين ناقص (زي "الاسم بالكامل")، والعميل رد برسالة قصيرة (كلمة لحد 4-5 كلمات) بدون سياق تاني واضح، اعتبرها إجابة مباشرة على أول حقل ناقص في القائمة.
2. تحديدًا لـ full_name: أي رسالة قصيرة (2-4 كلمات، كله حروف عربي/إنجليزي، من غير أرقام كتير) بعد سؤال "الاسم بالكامل" تتعتبر full_name = نص الرسالة كامل (زي "كيرلس ناجي فهيم")، مع تنظيف كلمة "الاسم" لو العميل كتبها في الأول (زي "الاسم كيرلس ناجي فهيم").
3. اختبار حي: إعادة نفس المحادثة (رد باسم قصير بعد سؤال الاسم) والتأكد إن `full_name` بيتحفظ من أول مرة ومايتكررش السؤال.

---

## F6 — ذكاء الشات في فهم السياق والهجة المصرية ✅ (نسخة أولى اتنفذت)

**اللي اتعمل فعليًا:**
1. تأكيد بسيط ("تمام"/"ماشي"/"اوك"...) بعد ما البوت يعرض مكنة/مكن (مش بس بعد حساب قسط زي قبل كده) بقى بيتفهم كـ"كمّل معايا في اللي عرضته" مش طلب جديد من الصفر.
2. تضييق عام (زي "خليك في VLR") من نفس المكن اللي اتعرضت فعلاً بقى بيشتغل — مش بس لكلمتين "استيراد"/"فرز تاني" زي قبل كده. اتعمل واتأكد بتجربة حية على بيانات حقيقية (3 مكن دايو → ضيّق صح لواحدة بس).
3. الـ prompt بتاع `AiIntentClassifier::classify()` (اللي بيتنادى أصلاً على كل رسالة، فمفيش تكلفة إضافية حقيقية) اتضاف له قواعد صريحة لنفس الفكرتين، عشان الحالات اللي heuristic الكود المحلي مايغطيهاش (رسايل أطول من 3 كلمات مثلاً) لسه الـ AI نفسه يقدر يفهمها من السياق.

**لسه مفتوح / يحتاج ملاحظة حية:** التحسين ده مبني على الأمثلة اللي وصفتها بالظبط — محتاج تجربة على محادثات حقيقية أكتر عشان نشوف هل فيه صيغ تانية لسه بتضيع، ونضيفها.

---

## F6 (الخطة الأصلية، محفوظة للسياق)

**المشكلة (بالأمثلة اللي انت وصفتها):**
1. الـ AI يرشح مكنة، العميل يرد "ماشي تمام" (تأكيد بسيط)، فيرد عليه "عاوز مكنه ايه؟" — ناسي إنه هو نفسه رشحها قبل شوية.
2. الـ AI يرشح أكتر من مكنة، العميل يضيّق ويقول "خليك في VLR"، فبدل ما يضيّق **من نفس اللي رشحه**، يعمل بحث جديد على كل مكن الـ VLR في الكتالوج كله.

**السبب الجذري (من قراءة الكود الفعلي، مش تخمين):**
- التأكيد/المتابعة بيتكتشفوا بقوائم كلمات ثابتة (`isPureFollowUp`, `isBareConfirmation`, `isBareConfirmationAfterCalc` في [`WhatsappIntentRouter.php`](app/Services/WhatsappIntentRouter.php)) — كل قايمة بتغطي صيغ محددة بس، ومش بتتعمم على كل صياغات اللهجة المصرية.
- `isBareConfirmationAfterCalc` تحديدًا شغالة بس لما `last_topic === 'installment_calc'` — يعني تأكيد بعد "ترشيح مكنة" (مش حساب قسط) مش متغطي خالص، فده بالظبط سبب المثال الأول.
- تضييق النطاق (`narrowMachinesByVariant`) بيشتغل بس لكلمتين محددتين ("استيراد"/"فرز تاني") — مفيش منطق عام "لو العميل كتب كلمة بتطابق واحدة من المكن اللي عرضناها بالفعل، ضيّق منها هي مش هات كل حاجة في الداتابيز بنفس الاسم" — ده سبب المثال التاني (VLR).
- التصميم الحالي بيعتمد على heuristics محلية (كود PHP) بالأولوية، والـ AI classify بيتنادى غالبًا من غير ما ياخد `last_machines` (المكن اللي اتعرضت فعلاً) كجزء أساسي من قراره — فحتى لو الـ AI فهم اللهجة صح، معندوش "الذاكرة" اللي يربط بيها.

**3 اتجاهات ممكنة (المقترح: الأول):**

**أ) (موصى بيه) تقوية السياق اللي بيوصل للـ AI + قاعدة "افتراض المتابعة"**
بدل ما نضيف كلمات جديدة لقوايم ثابتة كل ما يظهر تعبير جديد (حل مالوش نهاية)، خلي `classify()` ياخد دايمًا: آخر مكن اتعرضت (`last_machines`) + آخر موضوع (`last_topic`) + آخر 3-5 رسائل، مع تعليمات صريحة في الـ prompt: "لو رسالة العميل قصيرة/غامضة ومفيهاش طلب واضح جديد، وفيه مكن اتعرضت قبل كده في نفس المحادثة، اعتبرها بترد على اللي اتعرض (تأكيد أو تضييق) مش طلب جديد من الصفر." وده بالظبط شغل الـ AI الطبيعي (فهم لغة) بدل ما نحاول نغطي كل صيغة بكود.
- ميزة: بيتعمم على أي صياغة/لهجة تلقائيًا لأن الفهم بقى شغل الموديل مش قايمة كلمات.
- تكلفة: استدعاء AI أكتر في حالات كان بيتم حلها بـ heuristic محلي مجانًا — تعارض جزئي مع تخفيض التوكنز اللي اتعمل قبل كده (F مذكورة في [AI_TOKEN_USAGE_ISSUES.md](AI_TOKEN_USAGE_ISSUES.md))، فمحتاج توازن.

**ب) توسيع قوايم الكلمات الحالية (حل سريع لكن مؤقت)**
نضيف صيغ مصرية أكتر لكل قايمة (تمام، ماشي، تمام كده، اوك، ok، حلو، فوكيت...). بيحسّن الموقف بسرعة وبدون تكلفة AI إضافية، لكنه مش هيغطي كل الصيغ ولا هيحل مشكلة التضييق (VLR) لأنها مش مشكلة كلمات أصلاً، دي مشكلة منطق (البحث بيدور في الكتالوج كله بدل المرشحين بس).

**ج) الـ AI يقود كل قرار (إزالة الـ heuristics المحلية تمامًا)**
كل رسالة تروح لـ `classify()` بكل السياق من غير أي heuristic قبلها. أبسط من ناحية الصيانة، لكن بيرفع تكلفة التوكنز بشكل كبير (كل رسالة = استدعاء AI حتى أبسط رد) وبيقلل قابلية التنبؤ بسلوك البوت.

**التعديلات المقترحة بالتفصيل (لو اخترنا أ):**
1. تعميم "التأكيد بعد ترشيح": أي تأكيد قصير (مش مرتبط بكلمة معينة) بعد أي `last_topic` (مش بس `installment_calc`) لازم يتفهم كـ"كمّل مع اللي عرضته" مش "ابدأ من جديد".
2. تضييق من المرشحين الأصليين أولاً: قبل أي بحث جديد في الكتالوج، لو `last_machines` غير فاضية، شوف هل رسالة العميل بتطابق جزء من واحدة أو أكتر منها — لو أيوه، ضيّق من نفس المجموعة دي، ولو لأ اعمل بحث جديد عادي.
3. الـ prompt بتاع `classify()` ياخد قاعدة صريحة: "لو معندكش نية واضحة 100% والمحادثة فيها مكن معروضة، افترض إنه بيتكلم عنها."
4. لو الثقة (confidence) من الـ AI واطية والمكن المعروضة موجودة، الافتراضي يبقى "متابعة" مش "اسأله يقصد ايه".
5. (اختياري، مرحلة تانية) توحيد الـ heuristics المتناثرة (`isPureFollowUp`, `isBareConfirmation`, إلخ) في مكان واحد بدل ما تكون قوايم كلمات منفصلة في أماكن مختلفة، عشان أي تحسين مستقبلي يتعمل مرة واحدة مش في 5 أماكن.

**قرار محتاج منك قبل التنفيذ:** إحنا هنميل لـ(أ) بس فيه تريد-أوف تكلفة/جودة لازم تكون واعي بيه — هل موافق نزوّد استدعاءات AI شوية عشان الفهم يبقى أذكى، ولا تفضّل نبدأ بـ(ب) السريع الأرخص الأول ونشوف الفرق قبل ما نروح لـ(أ)؟

---

## الترتيب المقترح للتنفيذ

1. **F1** — أبسط مهمة وأوضح تشخيص، تصليح سريع.
2. **F5** — السبب معروف بالظبط، تعديل prompt واحد.
3. **F3** — تصليح منطقي محدد في مسار حساب القسط.
4. **F4** — يحتاج تشخيص إضافي بأداة `debug()` الموجودة قبل التعديل، لكن الاتجاه واضح.
5. **F2** — أكبر مهمة (ميزة جديدة كاملة)، تحتاج جلسة تصميم منفصلة قبل التنفيذ.
