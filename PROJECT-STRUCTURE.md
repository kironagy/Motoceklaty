# هيكل مشروع MotoCyklaty

هذا الملف يشرح مكونات المشروع ووظيفة كل مجلد وملف رئيسي بطريقة مختصرة.

## الفكرة العامة

المشروع عبارة عن نظام إدارة ومبيعات مبني على Laravel، ويحتوي على:

- لوحة تحكم إدارية مبنية باستخدام Filament.
- واجهة API وواجهات ويب لإدارة العملاء والطلبات والمخزون.
- تكامل مع WhatsApp لاستقبال الرسائل وإرسال الردود.
- خدمات ذكاء اصطناعي لتحليل الرسائل والردود والذاكرة.
- خدمة OCR منفصلة لقراءة صور المستندات وملفات PDF العربية.
- واجهة Frontend مبنية من ملفات JavaScript وCSS داخل `resources`.

## الشكل العام للمشروع

```text
project/
├── app/                  كود التطبيق الأساسي في Laravel
├── bootstrap/            ملفات بدء تشغيل Laravel والكاش
├── config/               إعدادات التطبيق والخدمات
├── database/             الهجرات والمصانع والبيانات التجريبية
├── front/                واجهة أو صفحة Frontend مستقلة
├── lang/                 ملفات الترجمة
├── ocr-service/          خدمة OCR مكتوبة بـ Python
├── public/               الملفات العامة التي يقرأها المتصفح
├── resources/            ملفات الواجهات وCSS وJavaScript المصدرية
├── routes/               تعريف مسارات الويب وAPI والأوامر
├── stubs/                قوالب إنشاء ملفات Filament
├── tests/                اختبارات المشروع
├── whatsapp-bot/         خدمة WhatsApp مكتوبة بـ Node.js
├── web/                  ملفات أو نسخ مرتبطة بالموقع المنشور
├── artisan               أداة أوامر Laravel
├── composer.json         حزم PHP وإعدادات Composer
├── package.json          حزم JavaScript وأوامر البناء
├── phpunit.xml           إعدادات اختبارات PHPUnit
├── vite.config.js        إعداد Vite لبناء ملفات Frontend
└── README.md             ملف التعريف الافتراضي بالمشروع
```

## مجلد `app`

يحتوي على منطق التطبيق المكتوب بلغة PHP.

### `app/Models`

نماذج قاعدة البيانات باستخدام Eloquent. كل نموذج يمثل كيانًا في النظام، مثل:

- `User.php`: المستخدمون والصلاحيات الأساسية.
- `ClientReview.php`: مراجعات العملاء.
- `Order.php` و`OrderItem.php`: الطلبات وتفاصيل المنتجات داخل الطلب.
- `InstallmentRequest.php`: طلبات التقسيط ومراحل مراجعتها.
- `Machine.php`: المكن أو المنتجات المعروضة للبيع.
- `Brand.php`: العلامات التجارية.
- `StockGroup.php` و`StockItem.php`: مجموعات المخزون وعناصره.
- `Staff.php`: الموظفون ومتابعة أدائهم.
- `WhatsappBot.php`: إعدادات حسابات أو روبوتات WhatsApp.
- `WhatsappConversation.php` و`WhatsappMessage.php`: المحادثات والرسائل.
- `AiMemory.php`: الذاكرة التي يستخدمها الذكاء الاصطناعي أثناء المحادثات.

### `app/Http/Controllers`

تستقبل الطلبات القادمة من المتصفح أو من الخدمات الخارجية، ثم تستدعي النماذج والخدمات المناسبة.

- `Api/`: Controllers خاصة بواجهات API.
- `Webhooks/`: استقبال Webhooks، ومنها رسائل وحالات WhatsApp.

### `app/Services`

منطق الأعمال القابل لإعادة الاستخدام بدل وضعه بالكامل داخل Controllers.

- خدمات الذكاء الاصطناعي: تصنيف النية، تحليل الردود، بناء سياق الذاكرة واستخراج بيانات الطلب.
- خدمات OCR: الاتصال بخدمة `ocr-service` وقراءة نتائج المستندات.
- خدمات WhatsApp: إرسال الرسائل ومعالجة المحادثات.
- خدمات مساعدة للتنبيهات والطلبات والمخزون.

### `app/Filament`

كل ما يخص لوحة الإدارة:

- `Pages/`: صفحات مخصصة في لوحة التحكم.
- `Resources/`: شاشات إدارة النماذج والبيانات.
- `Widgets/`: الإحصائيات واللوحات المختصرة، مثل ترتيب الموظفين.

### `app/Console`

أوامر Artisan المخصصة والمهام التي تعمل من سطر الأوامر أو بشكل دوري.

### `app/Providers`

تسجيل الخدمات والإعدادات وربط مكونات Laravel وFilament أثناء تشغيل التطبيق.

### `app/Tables/Columns`

أعمدة مخصصة لجداول Filament لإعادة استخدام تنسيق وعرض البيانات.

## مجلد `routes`

- `web.php`: مسارات صفحات الموقع والواجهات التي تفتح في المتصفح.
- `api.php`: مسارات API التي تستهلكها الواجهات أو الخدمات الخارجية.
- `console.php`: تعريف أوامر ومهام Laravel المجدولة.

## مجلد `database`

- `migrations/`: إنشاء وتعديل جداول قاعدة البيانات.
- `factories/`: إنشاء بيانات تجريبية للاختبارات والتطوير.
- `seeders/`: إدخال بيانات أولية أو افتراضية في قاعدة البيانات.

## مجلد `resources`

الملفات المصدرية التي يتم تطويرها قبل نشرها إلى `public`:

- `views/`: قوالب Blade وصفحات الموقع ومكونات Filament.
- `js/`: JavaScript الخاص بالواجهة.
- `css/`: CSS الخاص بالواجهة.

## مجلد `public`

هو المجلد العام الذي يمكن لخادم الويب الوصول إليه مباشرة.

- `index.php`: نقطة دخول Laravel.
- `build/`: ملفات JavaScript وCSS بعد البناء بواسطة Vite.
- `css/` و`js/`: ملفات Frontend المنشورة.
- `images/`: الصور العامة.
- `sw.js`: Service Worker، غالبًا للإشعارات أو خصائص PWA.
- `robots.txt` و`sitemap.xml`: إعدادات محركات البحث.

## مجلد `ocr-service`

خدمة مستقلة مكتوبة بـ Python باستخدام FastAPI وPaddleOCR.

وظيفتها قراءة صور المستندات وملفات PDF، خصوصًا المستندات العربية، ثم إعادة النص والبيانات المستخرجة إلى Laravel عبر HTTP.

- `app/`: كود خدمة OCR.
- `requirements.txt`: مكتبات Python المطلوبة.
- `README.md`: تعليمات تثبيت وتشغيل الخدمة.

عادة تعمل الخدمة على العنوان:

```text
http://127.0.0.1:8100
```

ويتصل بها Laravel من خلال `app/Services/Ocr/PaddleOcrClient.php`.

## مجلد `whatsapp-bot`

خدمة Node.js مسؤولة عن تشغيل أو إدارة اتصال WhatsApp.

- `index.js`: نقطة تشغيل الخدمة.
- `package.json`: مكتبات وأوامر Node.js.
- `auth/`: بيانات المصادقة الخاصة باتصال WhatsApp، ويجب التعامل معها كبيانات حساسة.

تتواصل الخدمة مع Laravel من خلال API وطلبات Webhook لمعالجة الرسائل والردود.

## مجلد `front`

يحتوي على صفحة أو واجهة Frontend مستقلة عن قوالب Laravel، ويبدأ تشغيلها من `index.html`.

## مجلد `tests`

- `Feature/`: اختبارات تدفق التطبيق، مثل API والطلبات والصلاحيات.
- `Unit/`: اختبارات الوحدات والخدمات الصغيرة.
- `TestCase.php`: الإعداد الأساسي لاختبارات Laravel.

## ملفات الإعداد الرئيسية

- `composer.json`: حزم PHP، ومنها Laravel وFilament، وأوامر Composer.
- `package.json`: حزم JavaScript وأوامر التطوير والبناء.
- `vite.config.js`: إعداد تجميع ملفات Frontend.
- `phpunit.xml`: إعداد PHPUnit.
- `artisan`: تنفيذ أوامر Laravel مثل الهجرات والكاش والاختبارات.
- `config/`: إعدادات قاعدة البيانات، البريد، التخزين، الذكاء الاصطناعي، OCR، والجلسات.

## مجلدات التشغيل المحلية

المجلدات التالية قد تكون ناتجة عن التشغيل المحلي أو أدوات خارجية وليست جزءًا من منطق التطبيق الأساسي:

- `chatgpt-profile/`: بيانات متصفح أو جلسة محلية.
- `chatgpt-worker/`: ملفات Worker مرتبطة بأتمتة ChatGPT.
- `sessions/`: ملفات جلسات أو بيانات تشغيل مؤقتة.
- `bootstrap/cache/`: كاش Laravel.

يفضل عدم تعديل أو مشاركة هذه المجلدات إلا عند الحاجة، والتأكد من عدم رفع بيانات المصادقة أو الملفات السرية إلى Git.

## مسار العمل المختصر

```text
المستخدم أو WhatsApp
        ↓
Routes أو Webhook
        ↓
Controllers
        ↓
Services + Models
        ↓
قاعدة البيانات / الذكاء الاصطناعي / OCR / WhatsApp
        ↓
رد API أو صفحة لوحة التحكم أو رسالة WhatsApp
```
