# ربط الواتساب بـ Google Cloud Vision API (OCR) — خطة العمل

3 Tasks رئيسية. كل Task ليها Status وتحتها شرح مدقق لِلي هيتعمل بالظبط.

---

## Task 1 — ربط Google Cloud Vision API كمزوّد OCR
**Status: 🔴 لسه متبداش (Not Started)**

### الوضع الحالي
- `config/ocr.php` فيه إعدادات PaddleOCR بس (`PADDLE_OCR_URL`, `PADDLE_OCR_TOKEN`...). مفيش `driver` ولا إعدادات Google.
- `app/Services/Ocr/OcrProviderInterface.php` عقد جاهز، و `MediaOcrHandler` بيستخدمه.
- `app/Services/Ocr/PaddleOcrClient.php` هو التنفيذ الوحيد الموجود دلوقتي، وبيتنادى مباشرة بدون binding قابل للتبديل.

### الي هيتعمل
1. **إضافة مكتبة Google** (`google/cloud-vision`) في `composer.json`.
2. **`config/ocr.php`**: إضافة `driver` (`env('OCR_DRIVER', 'paddle')`) وقسم `google_vision` بالمفاتيح دي (كلها من `.env`، انت هتملاها من الداشبورد/السيرفر، مش هتتكتب هنا):
   - `GOOGLE_VISION_PROJECT_ID`
   - `GOOGLE_APPLICATION_CREDENTIALS` (مسار ملف Service Account JSON على السيرفر — لازم يكون مفعّل عليه `Cloud Vision API` من نفس اللينك اللي بعتّه)
   - `GOOGLE_VISION_TIMEOUT`
3. **`GoogleVisionClient implements OcrProviderInterface`** جديد في `app/Services/Ocr/` — بيستخدم `TEXT_DETECTION` (أو `DOCUMENT_TEXT_DETECTION` — أفضل للمستندات الرسمية زي البطاقة ومفردات المرتب لأنه بيحافظ على ترتيب الأسطر والفقرات أحسن من `TEXT_DETECTION` العادي) ويرجّع النص المستخرج (raw OCR text)، بنفس شكل خرْج `PaddleOcrClient` عشان الباقي مايتأثرش.
   - **دعم العربي إجباري**: هيتحدد `imageContext.languageHints = ['ar']` في كل Request عشان الـ Vision يقرا المستندات العربية (بطاقة، مفردات مرتب، سجل تجاري...) صح، لأن من غير `languageHints` بيحصل خلط أحيانًا بين العربي ولغات تانية أو استخراج غلط للأرقام العربية/الهندية.
   - Error handling واضح لو الـ API رجّع فشل (quota, invalid image, timeout) — رسالة واضحة تتسجل في اللوج، ومايكسرش المحادثة على الواتساب.
4. **Binding في `AppServiceProvider`**: `OcrProviderInterface` يترّبط بـ `GoogleVisionClient` أو `PaddleOcrClient` حسب `config('ocr.driver')` — بدون ما نمسح Paddle، عشان لو حصل مشكلة نرجع بسرعة بـ `.env` بس من غير تعديل كود.
5. **Tests**: unit test لـ `GoogleVisionClient` (mock للـ API client، بما فيها التأكد إن `languageHints` بتتبعت `ar`) + test إن الـ binding بيرجع الكلاس الصح حسب الـ driver.
6. **توثيق مفاتيح الـ `.env`** في `.env.example` (بدون قيم حقيقية) عشان تعرف بالظبط تحط ايه من الداشبورد بتاع Google Cloud اللي بعتّه.

### ليه محتاجين ده
عشان التكامل يبقى "قوي ومظبوط" زي ما طلبت: مفيش هارد-كود، النص العربي يتقرا صح من أول مرة عن طريق `languageHints`، ولو الـ Google API وقع أو غيّرت الإعدادات، النظام يرجع لـ Paddle أو يديك خطأ واضح بدل ما يقف بصمت.

---

## Task 2 — تدفق جمع المستندات والتحقق منها عبر الواتساب (القسط/الكاش)
**Status: 🔴 لسه متبداش (Not Started)**

### الوضع الحالي
- `app/Services/Handlers/ApplicationHandler.php` بيجمع بيانات نصية بس (اسم، رقم قومي، تليفون، نوع وظيفة...) ومابيفرقش بين كاش وقسط، ومابيطلبش صور مستندات، ومابيعملش `InstallmentRequest`.
- جدول `ai_memories` فيه القواعد بالفعل جاهزة كنصوص (تقدر تديره من `/admin/ai-memories`) زي: "المستندات الأساسية المطلوبة"، "الموظفين"، "أصحاب المعاشات"، "أصحاب الأنشطة التجارية"، "أصحاب المهن الحرة"، "استخراج الاسم من البطاقه"، "مراجعه صور النشاط"، "تاكيد الرفع".
- `AiMemoryResolver` بيقدر يجيب هذه القواعد وقت الحاجة.

### الي هيتعمل
1. **تفرقة كاش/قسط**: لما العميل يوافق على الشراء، لو **كاش** → رد بسيط "تعالى المعرض". لو **قسط** → يبدأ تدفق جمع المستندات.
2. **تحديد فئة الدخل** من كلام العميل (موظف حكومي/خاص مؤمّن عليه/خاص مش مؤمّن/جيش/معاش/تجاري/مهنة حرة) — نفس المنطق الموجود حاليًا بس بيتوسّع.
3. **تحديد المستندات المطلوبة** حسب الفئة (فرد/محل) بقراءة القواعد من `ai_memories` بدل ما تتكتب في الكود — يعني لو غيّرت نص القاعدة من `/admin/ai-memories` السلوك يتغيّر من غير ما حد يلمس كود.
   - مثال: بطاقة رقم قومي + مفردات مرتب (موظف) / السجل التجاري + البطاقة الضريبية + صورة النشاط (محل).
4. **جمع صورة واحدة في كل مرة**: العميل يبعت صورة → `MediaOcrHandler` يبعتها لـ `GoogleVisionClient` (Task 1، بـ `languageHints=['ar']`) → النص الخام يتبعت لـ `DocumentDataExtractor` (جديد) اللي بياخد:
   - نص الـ OCR
   - نوع المستند
   - نص القاعدة المطلوب التحقق منها من `ai_memories`
   - ويرجّع بيانات مستخرجة + `valid: true/false` + سبب بالعربي لو مرفوض.
5. **قواعد التحقق (من `ai_memories`، مش هارد-كود)** — أمثلة القواعد اللي ذكرتها:
   - السن من 21 لـ 62 سنة (من البطاقة).
   - مفردات المرتب: تاريخ التحرير مايكونش عدّى عليه أكتر من 3 شهور.
   - تاريخ التعيين عدّى عليه 6 شهور على الأقل.
   - المرتب مايقلش عن 4000 جنيه.
   - أي قاعدة تانية مكتوبة في `/admin/ai-memories` هتتقرا وتتطبّق تلقائي.
6. **لو المستند رفض**: رد فوري بالعربي يوضح السبب ويطلب إعادة الرفع — **من غير ما يتعمل أي `InstallmentRequest`** لحد ما كل المستندات تعدي.
7. **لو المستند قبِل**: تتخزن بياناته والملف في حالة المحادثة، وينتقل للمستند اللي بعده.
8. **لما كل المستندات تعدي**: يتعمل صف `InstallmentRequest` فعلي بكل البيانات المستخرجة + الصور المرفوعة، `status = pending`، ويوصل فورًا على `/admin/deliveries` عشان تتم المراجعة الكاملة من الداشبورد. الرد للعميل: **"طلبك تحت التنفيذ"**.

### ليه محتاجين ده
ده اللي بيضمن إن كل المستندات اللي بترفع بالبيانات توصل جاهزة كاملة للمراجعة، وإن القواعد (السن، المرتب، التواريخ) تتغير من الداشبورد بس من غير تعديل كود، بالظبط زي ما طلبت.

---

## Task 3 — إشعارات تغيير حالة الطلب على الواتساب
**Status: 🔴 لسه متبداش (Not Started)**

### الوضع الحالي
- `InstallmentRequest` / `DeliveryResource` (صفحة `/admin/deliveries`) فيها عمود `status` (`new`, `pending`, `approved`, `rejected`, `paused`, ...) وعمود `checks_report` (سبب التوقف/الرفض) وActivity log جاهز.
- مفيش `Observer` classes في المشروع خالص لحد دلوقتي.
- مفيش آلية عامة لإرسال رسالة واتساب للعميل خارج سياق الرد المباشر على رسالة واردة.

### الي هيتعمل
1. **Migration**: إضافة عمود `whatsapp_conversation_id` (وربطه بالبوت) على `installment_requests` عشان نعرف نبعت الرد لمين بالظبط.
2. **`InstallmentRequestObserver`** جديد، مسجّل في `AppServiceProvider::boot()`، بيراقب `updating()` — لما `status` يتغيّر:
   - **approved**: رسالة تأكيد + "طلبك تم الموافقة عليه، تعالى الفرح/المعرض تكمل باقي الإجراءات".
   - **paused (متوقف)**: رسالة فيها **سبب التوقف** (من `checks_report`) + **المطلوب عمله** عشان يكمل.
   - **rejected**: رسالة فيها **سبب الرفض** لو موجود في `checks_report`، وإلا رسالة رفض عامة.
3. **Job منفصل (Queue)** `SendWhatsappStatusNotification` بيبعت الرسالة فعليًا عبر نفس الـ HTTP bridge الحالي (`{WHATSAPP_WORKER_URL}/send-message`) — بيتنفذ في الخلفية عشان حفظ الـ status في لوحة التحكم مايستناش رد الواتساب.
4. **Tests**: اختبار محتوى الرسالة لكل حالة (approved/paused/rejected) + اختبار إن تغيير أي عمود تاني غير `status` مابيبعتش رسالة.

### ليه محتاجين ده
بالظبط زي ما طلبت: أي تغيير في الـ status من `/admin/deliveries` يوصل فورًا للعميل على الواتساب برسالة مناسبة للحالة، وميستحملش أي تأخير على شاشة الداشبورد.

---

## ملاحظة عن `.env`
الملف ده مش هيحط أي قيم حقيقية. المفاتيح المطلوبة (هتتحط من الداشبورد بتاعك بعد تفعيل Cloud Vision API من الرابط اللي بعتّه):
```
OCR_DRIVER=google_vision
GOOGLE_VISION_PROJECT_ID=
GOOGLE_APPLICATION_CREDENTIALS=
GOOGLE_VISION_TIMEOUT=60
```
